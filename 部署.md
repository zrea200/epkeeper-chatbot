# 电管家智能客服聊天机器人 - 部署与使用文档

## 📖 项目简介

这是一个基于 React + Express 的智能客服聊天机器人系统，主要功能包括：

- **智能问答**：基于预设知识库的问答系统
- **语音识别**：集成讯飞语音识别（ASR），支持语音转文字
- **语音合成**：集成讯飞语音合成（TTS），支持文字转语音
- **NFC点位问答**：支持NFC点位触发的特定场景问答
- **微信小程序支持**：支持微信小程序登录和手机号解密
- **响应式设计**：适配PC和移动端

## 🛠️ 技术栈

### 前端
- **React 18+**：UI框架
- **TypeScript 5.6+**：类型安全
- **Vite 7+**：构建工具
- **Wouter**：路由
- **Tailwind CSS**：样式框架
- **Radix UI**：UI组件库

### 后端
- **Express 4+**：Web框架
- **Node.js 18+**：运行环境
- **TypeScript**：类型安全

### 部署
- **Docker**：容器化部署
- **Docker Compose**：多容器编排

### 第三方服务
- **讯飞语音API**：语音识别和合成
- **微信小程序API**：用户登录和授权

## 📋 环境要求

### 开发环境
- **Node.js**：18.0 或更高版本
- **pnpm**：10.4.1 或更高版本（项目已锁定版本）
- **操作系统**：Linux、macOS 或 Windows（推荐 Linux）

### 生产环境
- **Docker**：20.10 或更高版本
- **Docker Compose**：2.0 或更高版本
- **内存**：至少 512MB（推荐 1GB）
- **CPU**：至少 1 核（推荐 2 核）

## 🚀 快速开始

### 方式一：本地开发（推荐新手）

#### 1. 克隆项目
```bash
git clone <项目地址>
cd epkeeper-chatbot
```

#### 2. 安装依赖
```bash
# 如果没有安装 pnpm，先安装
npm install -g pnpm

# 安装项目依赖
pnpm install
```

#### 3. 配置环境变量
在项目根目录创建 `.env` 文件：

```bash
# 讯飞语音API配置（必须）
XUNFEI_APP_ID=your_app_id
XUNFEI_API_KEY=your_api_key
XUNFEI_API_SECRET=your_api_secret
```

**如何获取讯飞API密钥？**
1. 访问 [讯飞开放平台](https://www.xfyun.cn/)
2. 注册账号并登录
3. 创建应用，获取 `APPID`、`API_KEY`、`API_SECRET`
4. 开通语音识别和语音合成服务

#### 4. 启动开发服务器
```bash
# 同时启动前端和后端（推荐）
pnpm dev

# 或者分别启动
pnpm dev:frontend  # 前端（端口 5173）
pnpm dev:backend   # 后端（端口 3000）
```

#### 5. 访问应用
- 前端：http://localhost:5173
- 后端API：http://localhost:3000/api/health

### 方式二：Docker开发环境（适合熟悉Docker的用户）

```bash
# 启动开发环境（支持热重载）
pnpm deploy:dev

# 或使用 docker-compose 命令
docker-compose -f docker-compose.dev.yml up -d

# 查看日志
docker-compose -f docker-compose.dev.yml logs -f

# 停止服务
docker-compose -f docker-compose.dev.yml down
```

## 🏭 生产环境部署

### 方式一：PM2 部署（推荐，简单易用）

PM2 是一个 Node.js 进程管理器，适合生产环境，无需 Docker。

#### 1. 安装 PM2
```bash
# 全局安装 PM2
npm install -g pm2

# 或使用 pnpm
pnpm add -g pm2
```

#### 2. 准备环境变量
确保 `.env` 文件已配置完成（参考快速开始第3步）

#### 3. 构建项目
```bash
# 安装依赖（如果还没安装）
pnpm install

# 构建项目
pnpm build
```

#### 4. 启动服务
```bash
# 使用 PM2 启动（推荐）
pnpm pm2:start

# 或直接使用 PM2 命令
pm2 start ecosystem.config.cjs
```

#### 5. 常用 PM2 命令
```bash
# 查看状态
pnpm pm2:status
# 或
pm2 status

# 查看日志
pnpm pm2:logs
# 或
pm2 logs epkeeper-chatbot

# 重启服务
pnpm pm2:restart

# 停止服务
pnpm pm2:stop

# 删除服务
pnpm pm2:delete

# 更新代码后重新部署（构建+重启）
pnpm pm2:deploy
```

#### 6. 设置开机自启（可选）
```bash
# 生成启动脚本
pm2 startup

# 保存当前进程列表
pm2 save
```

#### 7. 验证部署
```bash
# 测试API
curl http://localhost:3000/api/health
```

#### 8. 访问应用
- 应用地址：http://localhost:3000
- API健康检查：http://localhost:3000/api/health

**注意**：默认端口是 `3000`，如需修改请编辑 `.env` 文件中的 `PORT` 变量。

### 方式二：Docker Compose部署

需要先安装 Docker 和 Docker Compose。

#### 1. 准备环境变量
确保 `.env` 文件已配置完成（参考快速开始第3步）

#### 2. 构建并启动
```bash
# 一键部署（构建镜像并启动容器）
pnpm deploy:prod

# 或使用 docker-compose 命令
docker-compose up -d --build
```

#### 3. 验证部署
```bash
# 检查容器状态
docker-compose ps

# 查看日志
docker-compose logs -f

# 测试API
curl http://localhost:51872/api/health
```

#### 4. 访问应用
- 应用地址：http://localhost:51872
- API健康检查：http://localhost:51872/api/health

**注意**：生产环境默认端口是 `51872`，如需修改请编辑 `docker-compose.yml` 文件。

### 方式三：手动运行（不推荐生产环境）

```bash
# 1. 安装依赖
pnpm install

# 2. 构建项目
pnpm build

# 3. 启动服务（前台运行，关闭终端会停止）
pnpm start
```

## 🔄 高级部署策略

### 蓝绿部署（零停机更新）

蓝绿部署可以在不中断服务的情况下更新应用。

#### 使用步骤：

1. **启动绿色环境（新版本）**
```bash
pnpm deploy:blue-green
# 或
docker-compose -f docker-compose.blue-green.yml up -d green
```

2. **测试绿色环境**
```bash
curl http://localhost:51873/api/health
```

3. **切换流量**
   - 更新负载均衡器配置，将流量指向绿色环境（端口 51873）
   - 或使用 Nginx/Traefik 等反向代理切换

4. **停止蓝色环境（旧版本）**
```bash
docker-compose -f docker-compose.blue-green.yml stop blue
```

### 金丝雀发布（灰度发布）

金丝雀发布可以逐步将流量切换到新版本，降低风险。

#### 使用步骤：

1. **启动金丝雀版本**
```bash
pnpm deploy:canary
# 或
docker-compose -f docker-compose.canary.yml up -d canary
```

2. **配置负载均衡器**
   - 10% 流量到 canary（端口 51874）
   - 90% 流量到 stable（端口 51872）

3. **监控和调整**
   - 监控金丝雀版本性能
   - 逐步增加流量（20% → 50% → 100%）
   - 如有问题，立即回滚

4. **回滚（如有问题）**
```bash
docker-compose -f docker-compose.canary.yml stop canary
```

## ⚙️ 配置说明

### 环境变量详解

#### 必须配置
| 变量名 | 说明 | 示例 |
|--------|------|------|
| `XUNFEI_APP_ID` | 讯飞应用ID | `5f8a9b2c` |
| `XUNFEI_API_KEY` | 讯飞API密钥 | `abc123def456` |
| `XUNFEI_API_SECRET` | 讯飞API密钥 | `xyz789uvw012` |

#### 可选配置
| 变量名 | 说明 | 默认值 |
|--------|------|--------|
| `PORT` | 服务端口 | `3000` |
| `NODE_ENV` | 环境模式 | `production` |

#### 调试配置
| 变量名 | 说明 | 默认值 |
|--------|------|--------|
| `XUNFEI_SWAP_KEYS` | 是否交换API_KEY和API_SECRET（用于调试鉴权问题） | `false` |

### 端口说明

| 环境 | 前端端口 | 后端端口 | 说明 |
|------|----------|----------|------|
| 开发环境（本地） | 5173 | 3000 | Vite开发服务器 |
| 开发环境（Docker） | 51872 | 3000 | Docker映射端口 |
| 生产环境 | - | 51872 | 统一端口，前端通过后端服务 |

## 📡 API接口文档

### 健康检查
```http
GET /api/health
```

**响应示例：**
```json
{
  "status": "ok",
  "timestamp": "2024-01-01T00:00:00.000Z"
}
```

### NFC点位问答
```http
GET /api/nfc?point=1&avatar=avatar1&question=问题内容
```

**参数说明：**
- `point`：点位编号（数字）
- `avatar`：头像标识（字符串）
- `question`：问题内容（URL编码）

**响应示例：**
```json
{
  "success": true,
  "point": 1,
  "avatar": "avatar1",
  "question": "问题内容",
  "answer": "答案内容",
  "nextQuestion": "下一个问题"
}
```

### NFC数据获取
```http
GET /api/nfc-data?point=1&avatar=avatar1
```

**响应示例：**
```json
{
  "success": true,
  "point": 1,
  "avatar": "avatar1",
  "pointData": {
    "questions": [...]
  }
}
```

### 讯飞语音识别（ASR）
```http
POST /api/asr/xunfei
Content-Type: application/json

{
  "base64": "音频base64编码",
  "format": "wav",  // 可选：wav 或 pcm
  "rate": 16000     // 可选：采样率，默认16000
}
```

**响应示例：**
```json
{
  "success": true,
  "text": "识别出的文字内容"
}
```

### 讯飞语音合成（TTS）
```http
POST /api/tts/xunfei
Content-Type: application/json

{
  "text": "要合成的文字",
  "vcn": "xiaoyu",    // 可选：发音人，默认xiaoyu
  "speed": 50,        // 可选：语速 0-100，默认50
  "pitch": 50,        // 可选：音调 0-100，默认50
  "volume": 50,       // 可选：音量 0-100，默认50
  "aue": "lame"       // 可选：音频格式，lame(MP3) 或 wav
}
```

**响应：** 返回音频二进制流


## 🗂️ 项目结构

```
epkeeper-chatbot/
├── client/                 # 前端代码
│   ├── src/
│   │   ├── components/     # React组件
│   │   ├── pages/          # 页面组件
│   │   ├── hooks/          # 自定义Hooks
│   │   ├── contexts/        # React Context
│   │   ├── lib/            # 工具库
│   │   └── data/           # 数据文件
│   ├── public/            # 静态资源
│   └── index.html         # HTML入口
├── server/                 # 后端代码
│   ├── index.ts           # Express服务器入口
│   └── xunfei-speech-api.ts  # 讯飞API封装
├── shared/                 # 共享代码和数据
│   └── qa-database.json    # 问答数据库
├── docker-compose.yml      # 生产环境配置
├── docker-compose.dev.yml  # 开发环境配置
├── docker-compose.blue-green.yml  # 蓝绿部署配置
├── docker-compose.canary.yml      # 金丝雀发布配置
├── Dockerfile              # 生产镜像构建文件
├── Dockerfile.dev          # 开发镜像构建文件
├── package.json            # 项目配置和依赖
├── pnpm-lock.yaml          # 依赖锁定文件
├── tsconfig.json           # TypeScript配置
├── vite.config.ts          # Vite构建配置
└── .env                    # 环境变量（需要创建）
```

## 🛠️ 常用命令

### 开发命令
```bash
pnpm dev              # 启动开发服务器（前端+后端）
pnpm dev:frontend     # 仅启动前端
pnpm dev:backend      # 仅启动后端
```

### 构建命令
```bash
pnpm build            # 构建生产版本
pnpm start            # 启动生产服务器（需要先构建，前台运行）
pnpm preview          # 预览生产构建
```

### 代码质量
```bash
pnpm check            # TypeScript类型检查
pnpm format           # 格式化代码（Prettier）
```

### PM2 部署命令（推荐）
```bash
pnpm pm2:start        # 启动服务（后台运行）
pnpm pm2:stop         # 停止服务
pnpm pm2:restart      # 重启服务
pnpm pm2:delete       # 删除服务
pnpm pm2:logs         # 查看日志
pnpm pm2:status       # 查看状态
pnpm pm2:deploy       # 构建并重启（更新代码后使用）
```

### Docker部署命令
```bash
pnpm deploy:dev       # Docker开发环境部署
pnpm deploy:prod      # Docker生产环境部署
pnpm deploy:blue-green # 蓝绿部署
pnpm deploy:canary    # 金丝雀发布
```

### Docker命令
```bash
# 查看容器状态
docker-compose ps

# 查看日志
docker-compose logs -f

# 停止服务
docker-compose down

# 重启服务
docker-compose restart

# 进入容器
docker-compose exec epkeeper-chatbot sh
```

## ❓ 常见问题

### 1. 依赖安装失败
**问题**：`pnpm install` 失败，提示网络错误

**解决方案**：
```bash
# 使用国内镜像源
pnpm config set registry https://registry.npmmirror.com/

# 或使用官方源（如果国内镜像有问题）
pnpm config set registry https://registry.npmjs.org/
```

### 2. 讯飞API鉴权失败
**问题**：调用讯飞API时返回鉴权错误

**解决方案**：
1. 检查环境变量是否正确配置
2. 尝试交换 `XUNFEI_API_KEY` 和 `XUNFEI_API_SECRET` 的值
3. 或在 `.env` 中添加 `XUNFEI_SWAP_KEYS=true`

### 3. Docker构建超时
**问题**：Docker构建时下载依赖超时

**解决方案**：
编辑 `Dockerfile`，取消注释以下行使用host网络：
```dockerfile
# network: host
# extra_hosts:
#   - "host.docker.internal:host-gateway"
```

### 4. 端口被占用
**问题**：启动时提示端口已被占用

**解决方案**：
```bash
# 查找占用端口的进程
lsof -i :3000  # Linux/macOS
netstat -ano | findstr :3000  # Windows

# 修改端口（在 .env 文件中）
PORT=3001
```

### 5. 前端路由404
**问题**：刷新页面后出现404错误

**解决方案**：
这是正常现象，因为前端使用客户端路由。确保：
1. 所有请求都通过后端服务器（不要直接访问前端端口）
2. 后端正确配置了静态文件服务


## 🔧 开发指南

### 添加新的API接口

1. 在 `server/index.ts` 中添加路由：
```typescript
app.post("/api/your-endpoint", async (req, res) => {
  try {
    // 处理逻辑
    res.json({ success: true, data: {} });
  } catch (err: any) {
    res.status(500).json({ error: err.message });
  }
});
```

2. 在前端调用：
```typescript
const response = await fetch('/api/your-endpoint', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ /* 数据 */ })
});
```

### 修改问答数据

编辑 `shared/qa-database.json` 文件，按照现有格式添加或修改问答数据。

### 自定义样式

项目使用 Tailwind CSS，可以直接在组件中使用 Tailwind 类名，或修改 `client/src/index.css`。

### 添加新的页面

1. 在 `client/src/pages/` 创建新页面组件
2. 在 `client/src/App.tsx` 中添加路由：
```typescript
import { Route } from 'wouter';
import YourPage from './pages/YourPage';

<Route path="/your-path" component={YourPage} />
```

## 📝 注意事项

1. **环境变量安全**：不要将 `.env` 文件提交到版本控制系统
2. **API密钥安全**：生产环境使用环境变量或密钥管理服务，不要硬编码
3. **CORS配置**：生产环境应限制 `Access-Control-Allow-Origin`，不要使用 `*`
4. **日志管理**：生产环境建议配置日志轮转和集中管理
5. **监控告警**：建议配置健康检查和监控告警系统
6. **备份策略**：如有数据库，定期备份数据

## 📞 获取帮助

如果遇到问题：
1. 查看本文档的常见问题部分
2. 检查日志输出：`docker-compose logs -f` 或控制台输出
3. 确认环境变量配置是否正确
4. 检查网络连接和防火墙设置

## 📄 许可证

MIT License

---

**最后更新**：2024年

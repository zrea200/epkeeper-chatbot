# 电管家智能客服聊天机器人 - 技术文档

## 📐 系统架构

### 整体架构图

```
┌─────────────────┐
│   用户浏览器    │
│  (微信/PC/移动) │
└────────┬────────┘
         │ HTTP/WebSocket
         │
┌────────▼─────────────────────────┐
│      Express 服务器              │
│  ┌─────────────────────────────┐  │
│  │  静态文件服务 (前端资源)    │  │
│  └─────────────────────────────┘  │
│  ┌─────────────────────────────┐  │
│  │  API 路由层                  │  │
│  │  - /api/health              │  │
│  │  - /api/nfc                 │  │
│  │  - /api/asr/xunfei          │  │
│  │  - /api/tts/xunfei          │  │
│  │  - /api/wechat/*            │  │
│  └─────────────────────────────┘  │
└────────┬──────────────────────────┘
         │
    ┌────┴────┬──────────┬──────────┐
    │         │          │          │
┌───▼───┐ ┌──▼───┐ ┌───▼───┐ ┌───▼───┐
│讯飞API│ │微信API│ │数据库 │ │CDN    │
└───────┘ └──────┘ └───────┘ └───────┘
```

### 技术栈详解

#### 前端架构

**React 18+**
- 使用函数式组件和 Hooks
- 支持并发特性（Suspense、Concurrent Mode）
- 严格模式开启

**状态管理**
- React Context API：全局状态（主题、用户信息等）
- React Query：服务端状态管理和缓存
- 本地状态：useState、useReducer

**路由**
- Wouter：轻量级路由库，类似 React Router
- 支持客户端路由和代码分割

**样式方案**
- Tailwind CSS：原子化CSS框架
- Radix UI：无障碍UI组件库
- CSS变量：主题切换支持

**构建工具**
- Vite：快速构建工具，支持HMR
- ESBuild：TypeScript编译
- PostCSS：CSS处理

#### 后端架构

**Express 4+**
- 中间件架构
- RESTful API设计
- 静态文件服务

**中间件栈**
```
请求 → 日志中间件 → CORS中间件 → 压缩中间件 → JSON解析 → 路由处理 → 响应
```

**API设计原则**
- RESTful风格
- 统一错误响应格式
- 请求日志记录
- 错误处理和异常捕获

## 🔌 核心功能实现

### 1. 语音识别（ASR）

**实现位置**：`server/xunfei-speech-api.ts`

**工作流程**：
```
音频数据(base64) 
  → 解码为Buffer 
  → 调用讯飞WebSocket API 
  → 接收识别结果 
  → 返回文本
```

**关键代码**：
```typescript
// 创建WebSocket连接
const wsUrl = `wss://iat-api.xfyun.cn/v2/iat?authorization=${auth}`;
const ws = new WebSocket(wsUrl);

// 发送音频数据
ws.send(JSON.stringify({
  common: { app_id: appId },
  business: { language: 'zh_cn', domain: 'iat' },
  data: { status: 1, format: 'audio/L16;rate=16000', audio: base64 }
}));
```

**配置参数**：
- `format`：音频格式（wav/pcm）
- `rate`：采样率（默认16000Hz）
- `language`：识别语言（中文）

### 2. 语音合成（TTS）

**实现位置**：`server/xunfei-speech-api.ts`

**工作流程**：
```
文本内容 
  → 调用讯飞HTTP API 
  → 接收音频流 
  → 返回Buffer
```

**关键代码**：
```typescript
const response = await fetch(ttsUrl, {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    common: { app_id: appId },
    business: { aue: 'lame', vcn: 'xiaoyu' },
    data: { text: Buffer.from(text).toString('base64') }
  })
});
```

**配置参数**：
- `vcn`：发音人（xiaoyu/xiaoyan等）
- `speed`：语速（0-100）
- `pitch`：音调（0-100）
- `volume`：音量（0-100）
- `aue`：音频格式（lame=MP3, wav=WAV）

### 3. NFC点位问答

**数据存储**：`shared/qa-database.json`

**数据结构**：
```json
{
  "nfcPoints": [
    {
      "point": 1,
      "avatar": "avatar1",
      "questions": [
        {
          "question": "问题内容",
          "answers": ["答案1", "答案2"]
        }
      ]
    }
  ]
}
```

**匹配逻辑**：
1. 根据 `point` 和 `avatar` 查找点位数据
2. 问题匹配支持：
   - 精确匹配
   - 规范化匹配（去除标点、空格，忽略大小写）
3. 随机返回一个答案
4. 返回下一个推荐问题

### 4. 微信小程序集成

**登录流程**：
```
小程序调用 wx.login() 
  → 获取 code 
  → 发送到后端 /api/wechat/login 
  → 后端调用微信API换取 openid 和 session_key 
  → 返回给前端
```

**手机号解密**：
```
小程序调用 wx.getPhoneNumber() 
  → 获取 encryptedData 和 iv 
  → 发送到后端 /api/wechat/decrypt-phone 
  → 后端使用 session_key 解密 
  → 返回手机号
```

**安全注意事项**：
- `session_key` 不应返回给前端
- 手机号解密应在服务端完成
- 使用HTTPS传输敏感数据

## 🗄️ 数据流

### 前端数据流

```
用户操作 
  → React组件 
  → Hooks/Context 
  → API调用 (fetch/axios) 
  → 后端API 
  → 更新状态 
  → 重新渲染
```

### 后端数据流

```
HTTP请求 
  → Express中间件 
  → 路由处理 
  → 业务逻辑 
  → 第三方API调用 
  → 数据处理 
  → JSON响应
```

## 🔐 安全机制

### 1. 环境变量管理
- 敏感信息存储在 `.env` 文件
- `.env` 不应提交到版本控制
- Docker环境变量注入

### 2. CORS配置
- 开发环境：允许所有来源（`*`）
- 生产环境：应限制特定域名

### 3. 请求验证
- API参数验证
- 错误信息不泄露敏感数据
- 请求日志记录

### 4. 数据加密
- 微信数据使用AES-128-CBC加密
- 传输使用HTTPS（生产环境）

## 📦 构建流程

### 开发构建
```
源代码 
  → Vite HMR 
  → 内存中编译 
  → 浏览器热更新
```

### 生产构建
```
源代码 
  → Vite构建 (前端) 
  → ESBuild打包 (后端) 
  → 输出到 dist/ 
  → Docker镜像构建 
  → 容器运行
```

**构建步骤**：
1. `pnpm install`：安装依赖
2. `pnpm build`：构建前端和后端
3. Docker多阶段构建：
   - 阶段1：安装依赖和构建
   - 阶段2：复制构建产物，创建运行镜像

## 🐳 Docker部署详解

### 多阶段构建

**阶段1：构建阶段**
```dockerfile
FROM node:20-alpine AS builder
# 安装pnpm
# 复制依赖文件
# 安装依赖
# 复制源代码
# 构建项目
```

**阶段2：运行阶段**
```dockerfile
FROM node:20-alpine AS runner
# 复制构建产物
# 复制node_modules
# 创建非root用户
# 设置启动命令
```

**优势**：
- 最终镜像体积小（不包含构建工具）
- 安全性高（非root用户运行）
- 构建缓存优化

### 网络配置

**开发环境**：
- 端口映射：`51872:5173`（前端）、`3000:3000`（后端）
- 卷挂载：代码目录挂载，支持热重载

**生产环境**：
- 端口映射：`51872:3000`（统一端口）
- 静态文件：通过Express服务

### 健康检查

```dockerfile
HEALTHCHECK --interval=30s --timeout=3s \
  CMD node -e "require('http').get('http://localhost:3000', ...)"
```

## 🔍 调试技巧

### 前端调试

1. **浏览器控制台**
   - React DevTools
   - Network面板查看API请求
   - Console查看日志

2. **Vite调试**
   - HMR热更新
   - Source Map支持
   - 错误提示

3. **移动端调试**
   - vconsole：移动端控制台
   - 微信开发者工具

### 后端调试

1. **日志输出**
   - 请求日志：`[REQUEST]`
   - API日志：`[ASR-Xunfei]`、`[TTS-Xunfei]`等
   - 错误日志：包含堆栈信息

2. **Docker日志**
   ```bash
   docker-compose logs -f
   docker-compose logs -f epkeeper-chatbot
   ```

3. **API测试**
   ```bash
   # 健康检查
   curl http://localhost:3000/api/health
   
   # 测试语音识别
   curl -X POST http://localhost:3000/api/asr/xunfei \
     -H "Content-Type: application/json" \
     -d '{"base64": "..."}'
   ```

## ⚡ 性能优化

### 前端优化

1. **代码分割**
   - 路由级别代码分割
   - 动态导入组件

2. **资源优化**
   - 图片懒加载
   - CDN资源
   - 压缩静态资源

3. **缓存策略**
   - 静态资源长期缓存（hash文件名）
   - API响应缓存（React Query）

### 后端优化

1. **压缩中间件**
   - Gzip压缩（60-70%体积减少）
   - 仅压缩文本资源

2. **静态文件缓存**
   - 带hash的资源：1年缓存
   - 普通资源：1天缓存

3. **连接复用**
   - HTTP Keep-Alive
   - WebSocket连接复用（讯飞API）

## 🧪 测试策略

### 单元测试
- 使用 Vitest
- 测试工具函数和业务逻辑

### 集成测试
- API接口测试
- 端到端测试

### 手动测试清单
- [ ] 健康检查接口
- [ ] NFC问答功能
- [ ] 语音识别功能
- [ ] 语音合成功能
- [ ] 微信登录功能
- [ ] 前端路由跳转
- [ ] 移动端适配

## 📊 监控指标

### 应用指标
- API响应时间
- 错误率
- 请求量

### 系统指标
- CPU使用率
- 内存使用率
- 磁盘空间

### 业务指标
- 语音识别成功率
- 问答匹配率
- 用户活跃度

## 🔄 版本管理

### 版本号规则
- 主版本号.次版本号.修订号（如：1.0.0）
- 遵循语义化版本

### Git工作流
- `main`：生产环境分支
- `develop`：开发分支
- 功能分支：`feature/*`
- 修复分支：`fix/*`

### 发布流程
1. 代码审查
2. 测试验证
3. 构建镜像
4. 蓝绿/金丝雀部署
5. 监控验证
6. 全量发布

## 🛠️ 开发工具推荐

### IDE
- VS Code（推荐）
- WebStorm

### VS Code插件
- ESLint
- Prettier
- TypeScript
- Docker
- GitLens

### 命令行工具
- pnpm：包管理器
- docker：容器管理
- curl：API测试
- jq：JSON处理

## 📚 参考资料

### 官方文档
- [React文档](https://react.dev/)
- [Express文档](https://expressjs.com/)
- [Vite文档](https://vitejs.dev/)
- [TypeScript文档](https://www.typescriptlang.org/)
- [Docker文档](https://docs.docker.com/)

### 第三方服务
- [讯飞开放平台](https://www.xfyun.cn/doc/)
- [微信小程序文档](https://developers.weixin.qq.com/miniprogram/dev/framework/)

### 最佳实践
- RESTful API设计
- Docker最佳实践
- 前端性能优化
- 安全开发指南

---

**文档维护**：本文档应与代码同步更新，重大变更应及时更新文档。

